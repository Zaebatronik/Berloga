# 🤖 Развертывание Telegram Bot на Render.com

## 📋 Проблема
Бот работает только пока открыто окно PowerShell на вашем ПК. Если закрыть окно - бот останавливается.

## ✅ Решение
Развернуть бота на **Render.com** (бесплатно), чтобы он работал **24/7 без вашего участия**.

---

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка (УЖЕ ГОТОВО ✅)
- ✅ Файл `telegram-bot.js` обновлён
- ✅ Добавлена обработка ошибок
- ✅ Добавлен graceful shutdown
- ✅ URL обновлён на `https://a8a3ca83.kupyprodai.pages.dev`

### Шаг 2: Закоммитить изменения

```powershell
cd c:\Users\User\Desktop\fgh\KupyProdai
git add telegram-bot.js bot-package.json
git commit -m "feat: подготовка бота для развертывания на Render.com"
git push origin main
```

### Шаг 3: Создать новый Web Service на Render.com

1. **Откройте Render.com:** https://dashboard.render.com
2. **Нажмите "New +"** → **"Web Service"**
3. **Выберите репозиторий:** `KupiyProday`
4. **Настройки сервиса:**
   - **Name:** `kupyprodai-telegram-bot`
   - **Region:** `Frankfurt (EU Central)` или ближайший к вам
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым (корень проекта)
   - **Environment:** `Node`
   - **Build Command:** `npm install node-telegram-bot-api dotenv`
   - **Start Command:** `node telegram-bot.js`
   - **Instance Type:** `Free` (0$)

### Шаг 4: Добавить Environment Variables

В разделе **Environment** добавьте:

| Key | Value |
|-----|-------|
| `TELEGRAM_BOT_TOKEN` | `7939786678:AAHSujmve3UREb9YLpZZWY2fiA00qUj0Fz8` |
| `NODE_ENV` | `production` |

### Шаг 5: Deploy!

1. Нажмите **"Create Web Service"**
2. Подождите 2-3 минуты пока Render развернёт бота
3. В логах должно появиться:
   ```
   🤖 Telegram Bot запущен!
   📱 WebApp URL: https://a8a3ca83.kupyprodai.pages.dev
   ✅ Бот готов к работе 24/7
   ✅ Menu Button установлена!
   ```

### Шаг 6: Проверка

1. Откройте бота в Telegram
2. Напишите `/start`
3. Должна появиться кнопка "🛍️ Открыть Берлогу"

---

## ⚠️ ВАЖНО: Особенности Free Plan Render.com

**Free Plan засыпает через 15 минут неактивности.**

Но для Telegram бота это **НЕ ПРОБЛЕМА**, потому что:
- ✅ Бот использует **polling** (постоянное подключение к Telegram)
- ✅ Polling считается активностью
- ✅ Бот **НЕ ЗАСНЁТ** пока получает обновления от Telegram
- ✅ Работает 24/7 без перерывов

---

## 🔧 Альтернатива: Webhook вместо Polling

Если хотите **100% гарантию**, можно переключить бота на **webhook**:

### Преимущества webhook:
- ✅ Не требует постоянного подключения
- ✅ Более надёжно на Free Plan
- ✅ Меньше нагрузка на сервер

### Как переключить (опционально):

```javascript
// В telegram-bot.js заменить:
const bot = new TelegramBot(token, { polling: true });

// На:
const bot = new TelegramBot(token);
const PORT = process.env.PORT || 3000;

// Webhook URL (будет после создания сервиса)
const webhookUrl = 'https://kupyprodai-telegram-bot.onrender.com';

bot.setWebHook(`${webhookUrl}/bot${token}`);

// Express server для webhook
const express = require('express');
const app = express();
app.use(express.json());

app.post(`/bot${token}`, (req, res) => {
  bot.processUpdate(req.body);
  res.sendStatus(200);
});

app.get('/', (req, res) => {
  res.send('🤖 Telegram Bot is running!');
});

app.listen(PORT, () => {
  console.log(`Server listening on port ${PORT}`);
});
```

**НО:** Сейчас polling работает отлично, так что это не обязательно.

---

## 📊 Мониторинг

### Как проверить что бот работает:

1. **Render Dashboard:**
   - https://dashboard.render.com/web/[ваш-service-id]
   - Смотрите логи - должны быть сообщения от бота

2. **Telegram:**
   - Напишите боту `/start`
   - Должна появиться кнопка

3. **BotFather:**
   - Напишите @BotFather команду `/mybots`
   - Выберите вашего бота
   - Нажмите "Bot Settings" → "Group Privacy" - должно быть "Enabled"

---

## 🎯 Что делать СЕЙЧАС

### Вариант 1: Развернуть на Render (РЕКОМЕНДУЕТСЯ)

1. Выполните команды из Шага 2 (commit + push)
2. Зайдите на https://dashboard.render.com
3. Создайте новый Web Service (Шаг 3-5)
4. Проверьте что бот работает

**Время:** 5-10 минут  
**Результат:** Бот работает 24/7, можно закрыть PowerShell

### Вариант 2: Оставить локально (НЕ РЕКОМЕНДУЕТСЯ)

Если вы хотите оставить бота на локальном ПК:

1. **Windows:** Создайте задачу в Планировщике заданий
   - Запускать при старте системы
   - Команда: `node c:\Users\User\Desktop\fgh\KupyProdai\telegram-bot.js`

2. **Минусы:**
   - ❌ Бот работает только когда ПК включен
   - ❌ При перезагрузке нужно запускать заново
   - ❌ Если ПК выключен - бот не работает

---

## ✅ Итог

**Развертывание бота на Render.com - это:**
- ✅ Бесплатно
- ✅ Надёжно (99.9% uptime)
- ✅ Не требует вашего ПК
- ✅ Автоматические обновления при git push
- ✅ Логи и мониторинг

**Время настройки:** 5-10 минут  
**Результат:** Бот работает всегда, независимо от вашего ПК

---

## 🆘 Если что-то не работает

### Бот не запускается на Render:
1. Проверьте логи в Render Dashboard
2. Убедитесь что `TELEGRAM_BOT_TOKEN` добавлен в Environment Variables
3. Проверьте Build Command: `npm install node-telegram-bot-api dotenv`

### Бот не отвечает:
1. Проверьте что сервис **Running** (не Sleeping)
2. Откройте логи - должно быть "Бот запущен!"
3. Попробуйте перезапустить сервис (Manual Deploy)

### Нужна помощь:
- Render Docs: https://render.com/docs/web-services
- Telegram Bot API: https://core.telegram.org/bots/api

---

**Готово! Теперь бот будет работать 24/7 без вашего участия! 🎉**
